/**
 * Users listing page hooks (DataTables, filters, etc.).
 */
(function () {
  if (typeof document === "undefined") return;
  document.documentElement.setAttribute("data-theme-page", "users");
})();
